#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <glib.h>

struct edge {
	// from and to are inclusive indexes in the suffix text
	int from;
	int* to;
	node;
};

struct node {
	GArray *edges;
}

int main( void ){
	int step = 0;
	
	char c;
	while( (c = getchar()) != EOF){
		step++;
		
	}
}

